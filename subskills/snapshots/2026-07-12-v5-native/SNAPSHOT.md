# Native v5 domain-subskill snapshot

Schema: `v5-domain-1`

This immutable candidate snapshot was evolved locally from the preserved `2026-07-12` v4 domain snapshot. It is paired with the unselected v5 parent and is not active or installed. The v4 source snapshot remains byte-unchanged.

The parent owns orchestration and reliability controls. Each package here owns only marginal domain semantics: domain facts and gates, semantic work units/calibration, domain anomaly rivals/probes, protected domain action classes, change-impact edges, reviewers, simplifications, and behavior cases. Native packets cannot create agents, issue permits, choose commit mode, resume pauses, authorize effects, or mutate canonical state.

`manifest.sha256` pins every regular package file. Once the integrated candidate is accepted for evaluation, this snapshot must not be modified in place; a material successor uses a new sibling snapshot.
